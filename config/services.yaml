# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones

    App\DataPersister\UserDataPersister:
        arguments:
            $entityManager: '@doctrine.orm.entity_manager'
            $passwordHasher: '@security.user_password_hasher'
        tags: 
            - { name: 'api_platform.state_processor', args: [App\Entity\User] }

    App\DataPersister\CategoryDataPersister:
        arguments:
            $em: '@doctrine.orm.entity_manager'
            $security: '@security.helper'
        tags:
            - { name: 'api_platform.state_processor', args: [App\Entity\Category] }

    App\DataPersister\OperationDataPersister:
        arguments:
            $em: '@doctrine.orm.entity_manager'
            $security: '@security.helper'
        tags:
            - { name: 'api_platform.state_processor', args: [App\Entity\Operation] }

    App\EventListener\JWTCreatedListener:
        tags:
            - { name: kernel.event_listener, event: lexik_jwt_authentication.on_jwt_created, method: onJWTCreated }

    App\Doctrine\Extension\CurrentUserOperationExtension:
        arguments:
            $security: '@security.helper'
        tags:
            - { name: 'api_platform.doctrine.orm.query_extension.collection', priority: 10 }
            - { name: 'api_platform.doctrine.orm.query_extension.item', priority: 10 }

    App\Doctrine\Extension\CurrentUserCategoryExtension:
        arguments:
            $security: '@security.helper'
        tags:
            - { name: 'api_platform.doctrine.orm.query_extension.collection', priority: 10 }
            - { name: 'api_platform.doctrine.orm.query_extension.item', priority: 10 }
    